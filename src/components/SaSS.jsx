import '../Styles/Sass.css';

const Sass = (props) => {
    return(
        <div className="pageDetails">
            <h1>SaSS</h1>
            <br/>
            <main>
                <p> Would like to clean up the Styles folder so that scss files don't get too cluttered.</p>
            </main>
        </div>
    )
}

export default Sass